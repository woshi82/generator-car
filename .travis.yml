language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-act' ]; then cd .. && eval "mv $currentfolder generator-act" && cd generator-act; fi

